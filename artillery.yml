config:
  target: "http://3.139.186.169:6060"
  phases:
    - duration: 180
      arrivalRate: 550
      name: 550rps
  payload:
    path: "artillery.csv"
    fields:
      - "id"
scenarios:
  - name: "Browse workspaces"
    flow:
      - get:
          url: "/api/advanced/{{ id }}"